import { FormatRegNumPipe } from './format-reg-num.pipe';

describe('FormatRegNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRegNumPipe();
    expect(pipe).toBeTruthy();
  });
});
