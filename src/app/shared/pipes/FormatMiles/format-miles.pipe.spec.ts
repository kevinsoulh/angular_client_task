import { FormatMilesPipe } from './format-miles.pipe';

describe('FormatMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
